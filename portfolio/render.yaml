services:
  - type: web
    name: my-portfolio   # you can change to yourname-portfolio
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: "gunicorn portfolio.wsgi:application"
